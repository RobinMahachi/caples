% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caples_test.R
\name{caples_test}
\alias{caples_test}
\title{Read out an A/B/n test on a conversion rate}
\usage{
caples_test(
  data,
  group,
  outcome,
  ctrl,
  treatment = NULL,
  trials = NULL,
  conf_level = 0.95,
  adjust = "holm",
  alternative = c("two.sided", "greater", "less"),
  mde = NULL,
  mde_type = c("absolute", "relative"),
  power = 0.8,
  allocation = NULL,
  srm_threshold = 0.001,
  design = NULL
)
}
\arguments{
\item{data}{A data frame. Either one row per user (with a 0/1 outcome) or
one row per arm (with counts, see \code{trials}).}

\item{group}{Name of the column holding the arm labels.}

\item{outcome}{Name of the outcome column: 0/1 (or TRUE/FALSE) per user,
or the number of conversions per arm when \code{trials} is supplied.}

\item{ctrl}{The label of the control arm.}

\item{treatment}{Label(s) of the treatment arm(s). \code{NULL} (default) uses
every arm that isn't control.}

\item{trials}{For aggregated data: name of the column holding the number
of users/sends per arm. Leave \code{NULL} for row-level data.}

\item{conf_level}{Confidence level for intervals and significance.}

\item{adjust}{Multiplicity correction, any method accepted by
\code{\link[stats:p.adjust]{stats::p.adjust()}}: \code{"holm"}, \code{"bonferroni"}, \code{"BH"}, \code{"none"}, ...}

\item{alternative}{\code{"two.sided"}, \code{"greater"} or \code{"less"}, where
\code{"greater"} means treatment > control.}

\item{mde}{Optional target minimum detectable effect. If supplied, the
readout checks whether each arm reached the required sample size.}

\item{mde_type}{\code{"absolute"} or \code{"relative"}; see \code{\link[=caples_design]{caples_design()}}.}

\item{power}{Target power used for the sample-size check and achieved MDE.}

\item{allocation}{Optional intended traffic split. Either named by arm
label or ordered control first, then treatments. Enables the SRM check.}

\item{srm_threshold}{p-value below which SRM is flagged. The usual
convention is 0.001.}

\item{design}{Optional plan from \code{\link[=caples_design]{caples_design()}}, made before the test.
When supplied, \code{mde}, \code{mde_type}, \code{power}, \code{conf_level}, \code{alternative}
and (if not given) \code{allocation} are taken from the plan, and the
sample-size check compares each arm with the planned n. This is the
recommended workflow: decide first, analyse second.}
}
\value{
An object of class \code{caples_test}.
}
\description{
Compares one or more treatment arms against a control arm. Returns rates,
absolute and relative lifts with confidence intervals, raw and
multiplicity-adjusted p-values, an optional sample ratio mismatch (SRM)
check, a check on whether the test was big enough, and a plain-English
summary.
}
\details{
Confidence intervals are two-sided and are not adjusted for
multiple comparisons; only p-values are adjusted. The test is a pooled
two-proportion z-test, equivalent to \code{prop.test(correct = FALSE)}.
}
\examples{
set.seed(1)
df <- data.frame(
  arm = rep(c("control", "A", "B"), each = 5000),
  converted = c(rbinom(5000, 1, 0.050),
                rbinom(5000, 1, 0.056),
                rbinom(5000, 1, 0.062))
)
# Recommended: plan first, then pass the plan to the readout
plan <- caples_design(baseline = 0.05, mde = 0.01, power = 0.85, arms = 3)
caples_test(df, group = "arm", outcome = "converted", ctrl = "control",
           design = plan)

# Without a plan: supply the targets directly
caples_test(df, group = "arm", outcome = "converted", ctrl = "control",
           mde = 0.01, power = 0.85, allocation = c(1, 1, 1))
}

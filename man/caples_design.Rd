% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caples_design.R
\name{caples_design}
\alias{caples_design}
\title{Size an A/B/n test on a conversion rate}
\usage{
caples_design(
  baseline,
  mde,
  mde_type = c("absolute", "relative"),
  power = 0.8,
  arms = 2,
  allocation = NULL,
  conf_level = 0.95,
  alternative = c("two.sided", "greater", "less"),
  adjust_sizing = TRUE
)
}
\arguments{
\item{baseline}{Expected conversion rate in the control arm (between 0 and 1).}

\item{mde}{Minimum detectable effect.}

\item{mde_type}{\code{"absolute"} (percentage points, e.g. 0.01 = 5\% -> 6\%) or
\code{"relative"} (proportional change, e.g. 0.10 = 5\% -> 5.5\%).}

\item{power}{Target power, e.g. 0.8 or 0.85.}

\item{arms}{Total number of arms including control.}

\item{allocation}{Optional traffic split, control first, e.g.
\code{c(0.5, 0.25, 0.25)}. Defaults to an equal split. Rescaled to sum to 1.}

\item{conf_level}{Confidence level; significance level is \code{1 - conf_level}.}

\item{alternative}{\code{"two.sided"}, \code{"greater"} (treatment > control) or
\code{"less"} (treatment < control).}

\item{adjust_sizing}{If \code{TRUE} and there is more than one treatment arm,
alpha is divided by the number of comparisons (Bonferroni). This is the
conservative choice when you plan to use a multiplicity correction such
as Holm.}
}
\value{
An object of class \code{caples_design}: a list with the per-arm sample
sizes, total sample size and the inputs used.
}
\description{
Calculates the sample size needed per arm to detect a given minimum
detectable effect (MDE) against a control arm, for one or more treatment
arms, with equal or unequal traffic allocation.
}
\examples{
caples_design(baseline = 0.05, mde = 0.01)
caples_design(baseline = 0.05, mde = 0.10, mde_type = "relative",
             arms = 3, allocation = c(0.5, 0.25, 0.25))
}
